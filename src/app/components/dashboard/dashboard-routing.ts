import { Routes } from "@angular/router";
import { DashboardComponent } from "./dashboard.component";

/**
 * Routes for the dashboard component
 */
export const DashBoardRoutes: Routes = [
  { path: "", component: DashboardComponent }
];
